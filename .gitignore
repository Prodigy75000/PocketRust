# Build output
/target

# Deployed / built artifacts (regenerated by the build)
/deploy
/shots

# Editor / scratch
**/*.rs.bk
*.sav

# ROM images: ignore everywhere except the freely-distributable test ROMs
# (Blargg + dmg/cgb-acid2) that the test suite needs.
*.gb
*.gbc
!tests/roms/**/*.gb
!tests/roms/**/*.gbc
