[package]
name = "gb-libretro"
version = "0.1.0"
edition = "2021"

[lib]
# cdylib -> libgbcore_libretro.so; we rename to the libretro convention on deploy.
name = "gbcore_libretro"
crate-type = ["cdylib"]

[dependencies]
gb-core = { path = "../gb-core" }
