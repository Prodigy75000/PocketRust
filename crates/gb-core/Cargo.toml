[package]
name = "gb-core"
version = "0.2.2"
edition = "2021"
license = "GPL-3.0-or-later"
description = "A clean-room Game Boy (DMG) emulator core, written from scratch in Rust."

[lib]
name = "gb_core"
path = "src/lib.rs"

[dependencies]
